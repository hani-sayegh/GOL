// import logUpdate from 'log-update';
import Board from './Board';
// import colors from 'colors';

export default function displayBoard(board: Board)
{
    let b = board.toString();
    console.log(b);

}